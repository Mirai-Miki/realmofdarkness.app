// Slash command for creating a new chronicle entry
// this is the actual command cordinator but since creating a chronicle is share by multiple apps we will mostly just be using actions/services from the core package
