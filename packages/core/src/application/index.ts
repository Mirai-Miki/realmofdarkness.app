/**
 * Application layer exports.
 *
 * The application layer contains services that orchestrate domain logic
 * and implement use cases. Services depend on port interfaces defined
 * in the ports layer, not on concrete implementations.
 */

export * from "./services";
