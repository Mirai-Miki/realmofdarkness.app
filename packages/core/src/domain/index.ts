/**
 * Barrel exports for the domain package.
 *
 * Re-export domain modules (models, services, interfaces) so other packages
 * can import from "@core/domain" without referencing deep paths.
 *
 * @packageDocumentation
 */

export * from "./entities";
export * from "./value-objects";
